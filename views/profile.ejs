<!DOCTYPE html>
<html>

<head>
  <title>Profile</title>
</head>

<body>

  <% if (data) { %>
  <h1>Welcome, <%= data.displayName %>!</h1>
  <p>ID: <%= data.id %></p>
  <p>Full name: <%= data.displayName %></p>
  <p>Email: <%= data.email %></p>
  <img src=<%= data.picture %> alt="picture">

  <form action="/logout" method="POST">
    <button>Logout</button>
  </form>
  <% } else { %>
  <p>Please login.</p>

  <button onclick="location.replace('/')">Login</button>
  <% } %>
</body>

</html>